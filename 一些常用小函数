isset()   检测变量是否已设置并且非 NULL

var_dump()  打印变量的相关信息

print_r()   打印关于变量的易于理解的信息。

list(var) = array   把数组中的值赋给一组变量

each(array)    返回数组中当前的键／值对并将数组指针向前移动一步

unset(var)  删除/释放给定的变量

array_diff()  计算数组的差集, 比较两个数组不同的地方

foreach(array as $value) {}  遍历数组

eval("") 把字符串作为PHP代码执行

exit()  输出一个消息并且退出当前脚本

在函数原型中，`$...` 表示*等等*的意思。当一个函数可以接受任意个参数时使用此变量名。

is_type()  返回true/false
